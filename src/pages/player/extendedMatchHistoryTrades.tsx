import * as React from "react";

export const ExtendedMatchHistoryTrades = ({ extendedMatchData } : { extendedMatchData: any }) => {

    //const kills = extendedMatchData?.data.kills

    return (
        <div className="flex flex-row flex-wrap gap-4 justify-center font-bold uppercase text-xl">
           Soon™
        </div>
    )
}