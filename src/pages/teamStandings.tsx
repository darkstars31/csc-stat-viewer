// import React from "react";
// import { useDataContext } from "../DataContext";
// import { useCscSeasonMatches } from "../dao/cscSeasonMatches";



export function TeamStandings() {
    //const { dataConfig } = useDataContext();
    //const [ selectedTier, setSelectedTier ] = React.useState('Contender');
    //const { data: x = [] } = useCscSeasonMatches(selectedTier, dataConfig?.season);

    //console.info( x );
    //const teams = x.reduce( (acc, match) => { match.teamStats.map( teamStat => acc.push(teamStat.name)); return acc;}, [] as string[]);

    //console.info( teams );

    // const p = map.

    // x.reduce( (acc, match) => {

    //     const winning = match.
    //     const losing = 
    //     acc.push({})

    //     return acc;
    // }, [] as any);


    return null;
}